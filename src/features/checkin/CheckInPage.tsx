// TODO: Wire this up to the API client once types are generated.
// Flow: search → select person → check-in → see in arrival list

export function CheckInPage() {
  return (
    <main className="p-4">
      <h1 className="text-2xl font-semibold mb-4">Check-in</h1>
      <p className="text-muted-foreground">Coming soon.</p>
    </main>
  );
}
