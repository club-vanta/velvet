VITE_API_BASE_URL=https://api-alter-tracker.club-vanta.com

# Set to "true" to expose the dev server on all network interfaces (LAN access).
# Useful for testing from a phone or another device on the same network.
# Example: VITE_HOST_ALL=true npm run dev
# Or add this line to .env.local (gitignored).
# VITE_HOST_ALL=true
